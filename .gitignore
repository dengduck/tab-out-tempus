node_modules/
*.log
*.db
.DS_Store
docs/superpowers/

# Personal config (landing page patterns, etc.) — never push to GitHub
extension/config.local.js

# AI agent working memory — local only
.workbuddy/

# Cross-review internal files — not for public repo
docs/review/

# Build artifacts — use git tag snapshots instead
extension.zip
