/**
 * ui/utils/domain.js
 * -------------------
 * URL → hostname 分组规则。包含：
 *   - 标准 hostname 提取
 *   - localhost:port 分组
 *   - Homepages 判定（HOMEPAGE_HOSTS + 根路径）
 *
 * 里程碑：M2。
 */

/** @param {string} url */
export function getHostname(_url) { /* stub, M2 */ return ''; }

/** @param {string} url */
export function isHomepage(_url) { /* stub */ return false; }
