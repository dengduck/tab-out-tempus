/**
 * ui/views/historyView.js
 * ------------------------
 * Today / Week / Month / Year 切换视图。按需加载（点开才订阅 + 拉数据）。
 *
 * 里程碑：M6。
 */

export function onOpen(_loader) { /* stub, M6 */ }
export function render(_data) { /* stub */ }
export function close() { /* stub */ }
