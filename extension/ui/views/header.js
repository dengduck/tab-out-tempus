/**
 * ui/views/header.js
 * -------------------
 * "今日工作"时长 + pauseReasons / privateMode / focusTimer 状态指示。
 *
 * 接口：
 *   render(initial)
 *   updateTodayMs(ms)         —— BCAST_TICK 带动
 *   updateStateBadges(state)  —— BCAST_STATE_CHANGE 带动
 *
 * 里程碑：M5（时间 UI）。
 */

export function render(_initial) { /* stub, M5 */ }
export function updateTodayMs(_ms) { /* stub */ }
export function updateStateBadges(_state) { /* stub */ }
