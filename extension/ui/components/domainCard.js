/**
 * ui/components/domainCard.js
 * ----------------------------
 * 域名卡片容器（含 favicon、域名标题、总时长、close-all 按钮、内部 tab chips）。
 *
 * 里程碑：M2。
 */

export function create(_hostname, _tabs) { /* stub, M2 */ return null; }
