/**
 * ui/components/tabChip.js
 * -------------------------
 * 单个 tab chip（标题 + 时长 badge + 关闭按钮）。
 *
 * 里程碑：M2（基础） + M5（badge）。
 */

export function create(_tabInfo) { /* stub, M2 */ return null; }
export function updateBadge(_el, _ms) { /* stub, M5 */ }
